<h2>Upload asset file for <%= @site.name -%> site</h2>

<% form_for(@asset, :url => admin_site_assets_path, :html =>
  {:multipart => true}) do |f| %>
  <div class="record_fields">
    <%= f.error_messages %>

    <p>
      <%= f.label :file, 'Asset file' %><br />
      <%= f.file_field :file %>
    </p>
    <p>
      <%= f.label :label %><br />
      <%= f.text_field :label, :size => 46 %>
    </p>

    <p>
      <%= f.submit "Create" %>
    </p>
  </div>
<% end %>

<hr />

<div class="page_actions">
  <%= link_to 'Back to assets list', admin_site_assets_path(@site) %>
</div>
